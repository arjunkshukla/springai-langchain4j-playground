# Spring AI MCP Resource Server

This document is served from the local filesystem through the Model Context Protocol.

The server exposes local files as read-only resources. Add another text file to this directory, then request the catalog resource to discover it.
